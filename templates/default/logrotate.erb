<%= @path.join(" ") %> {
  <%= @frequency %>
  missingok
  rotate <%= @rotate %>
  compress
  delaycompress
  notifempty
  create <%= @create %>
  sharedscripts
  postrotate
    # Send SIGUSR2 signal to tell supervisord to reopen all log files.
    kill -SIGUSR2 `supervisorctl pid`
  endscript
}
